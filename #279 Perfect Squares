class Solution {
public:
	int numSquares(int n) {
		static vector<int> dp{0};
		while (dp.size() <= n) {
			int m = dp.size();
			int cur = INT_MAX;
			for (int i = 1; i * i <= m; ++i) {
					cur = min(cur, 1 + dp[m - i * i]);
			}
			dp.push_back(cur);
		}
		return dp[n];
	}
};
